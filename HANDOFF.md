# Codex引き継ぎ — メシ決めルーレット

## 目的

食事を決める迷いを減らし、一人でも複数人でも短時間で納得できる候補を出す。スマートフォンでの実用性を最優先しつつ、毎日使って心地よい、落ち着いたおしゃれさを持たせる。

## 現在地

- 実装済みのWebアプリがこのリポジトリにある。
- 公開中サイト: <https://meshi-kimeru.genomu37.chatgpt.site/>
- UI改善用の操作可能なモック: [docs/mockups/meshi-ui-v2-preview.html](docs/mockups/meshi-ui-v2-preview.html)
- Figma: <https://www.figma.com/design/PvhUPe0BwZElnHhCmCGNsU>
- 仕様: [docs/project/product-spec.md](docs/project/product-spec.md)
- 実装計画: [docs/project/implementation-plan.md](docs/project/implementation-plan.md)
- 直近の検証記録: [docs/project/implementation-review.md](docs/project/implementation-review.md)

## 決定済みの製品方針

- 一人利用と複数人利用の両方を扱う。
- 複数人利用は一台の端末だけで完結させる。
- 参加リンク、QRコード、複数端末同期は初版に含めない。
- 複数人では各候補の希望人数とNGを入力できる。
- 抽選方式は「平等」と「希望人数を反映」から選べる。
- 重み付き抽選は多数決ではなく、0票にも1口を残す。
- アカウント登録、独自の位置情報取得、サーバー保存は行わない。
- 結果からGoogleマップの周辺検索へ進める。

## UI・UX方針

- 実用性を損なわず、使っていて心地よいおしゃれさを加える。
- モバイルファースト。特に360pxと390pxを優先する。
- 主要操作は迷わせず、最初の画面から短い導線で抽選できるようにする。
- 主ボタン、結果、再抽選、条件変更の優先順位を明確にする。
- 派手な装飾より、余白、文字組み、階層、触ったときの反応で品質を出す。
- 選択、NG、無効状態を色だけに依存させない。
- 動きは短くし、`prefers-reduced-motion`を尊重する。

## UI作業の必須ルール

1. UIを変更する前に、既存実装とUIモックを比較する。
2. 変更後は必ず実行可能なモバイルプレビューをCodexの右側へ表示する。
3. 360pxと390pxで主要導線を操作し、横スクロールとラベル切れを確認する。
4. 利用者の確認前に公開中サイトを更新しない。
5. 承認後にのみ公開し、公開URLを実機で再確認する。

## 次の作業

操作可能なUIモックの方向性を、既存のReact実装へ反映する。

優先順:

1. 初期画面、抽選結果、複数人画面の情報階層をモックへ合わせる。
2. モバイルでのタップ数とスクロール量を減らす。
3. 条件、除外、履歴を必要なときだけ展開する。
4. 既存の抽選ロジック、保存、アクセシビリティを維持する。
5. 自動試験、lint、ビルドを実行する。
6. 右側のプレビューで確認を受ける。公開はまだ行わない。

## Codexへ渡す開始プロンプト

> `HANDOFF.md`、製品仕様、現在の実装、UIモックを確認してください。UIモックの方向性を既存のReact実装へ反映し、抽選ロジックと保存仕様は維持してください。変更後はテスト・lint・ビルドを実行し、360pxまたは390pxの操作可能なプレビューをCodexの右側に表示してください。確認を受けるまで公開しないでください。
